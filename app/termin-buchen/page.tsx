import Link from "next/link";
import MapEmbed from "../components/MapEmbed";
import SiteFooter from "../components/SiteFooter";
import SiteHeader from "../components/SiteHeader";
import { hours, services, site } from "../chaarisma-website-2026-data";

export default function BookingPage() {
  return <main className="inner-page booking-page"><SiteHeader inner /><section className="inner-hero"><div className="inner-hero-copy"><p className="eyebrow">Online-Terminbuchung · Chaarisma</p><h1>Termin buchen.</h1><p>Die vollständige Buchungsstrecke von Chaarisma bleibt erhalten. Hier findest du die wichtigsten Informationen gesammelt und gelangst mit einem Klick direkt zur Originalseite.</p><div className="hero-actions"><a className="button button-primary" href={site.bookingUrl}>Online-Termin buchen</a><Link className="text-link" href="/oeffnungszeiten/">Kontakt ansehen <span aria-hidden="true">→</span></Link></div></div><div className="inner-hero-mark" aria-hidden="true"><span>CH</span></div></section><section className="booking-page-intro"><p className="section-kicker">So funktioniert es</p><h2>Dienstleistung. Termin. Buchung.</h2><p>Die bestehende Landingpage führt durch die Schritte „Dienstleistung“, „Termin“ und „Buchung“. Dort kannst du auch für eine andere Person buchen und dich bei Bedarf einloggen.</p><a className="button button-primary" href={site.bookingUrl}>Originale Buchungsseite öffnen</a></section><section className="services-section booking-services" aria-labelledby="booking-services-title"><div className="section-heading section-heading-light"><p className="section-kicker">Dienstleistung auswählen</p><h2 id="booking-services-title">Alle Bereiche aus der Buchung.</h2><p>Die Kategorien entsprechen der bestehenden Chaarisma-Landingpage.</p></div><div className="service-list">{services.map((service) => <article className="service-row" key={service.number}><span>{service.number}</span><h3>{service.title}</h3><p>{service.copy}</p><a href={site.bookingUrl}>Auswählen <span aria-hidden="true">↗</span></a></article>)}</div></section><section className="booking-details-grid" aria-label="Termin- und Saloninformationen"><div className="booking-details-card booking-details-card-dark"><p className="section-kicker">Öffnungszeiten</p><h2>Wann du uns findest.</h2><dl className="contact-hours">{hours.map(([day, time]) => <div key={day}><dt>{day}</dt><dd>{time}</dd></div>)}</dl></div><div className="booking-details-card"><p className="section-kicker">Adresse &amp; Kontakt</p><h2>Saynische Str. 67. Daaden.</h2><p className="inner-address">{site.name}<br />{site.owner}<br />{site.address[0]}<br />{site.address[1]}</p><a className="button button-primary" href={site.directionsUrl} target="_blank" rel="noreferrer">Route öffnen</a><a className="booking-details-phone" href={site.phoneHref}>{site.phoneDisplay}</a></div></section><section className="booking-map-section"><div><p className="section-kicker">Anfahrt</p><h2>Bis bald in Daaden.</h2><p>Die Karte wird erst nach einem Klick geladen.</p></div><MapEmbed directionsUrl={site.directionsUrl} embedUrl={site.mapEmbedUrl} title="Karte zum Salon Chaarisma in Daaden" /></section><section className="inner-note-section"><p className="section-kicker">Weiter zur Buchung</p><h2>Die Originalseite wartet auf dich.</h2><p>Öffne jetzt dnfk.mitdenkt.io, um die verfügbaren Dienstleistungen und Termine von Chaarisma zu sehen.</p><a className="button button-primary" href={site.bookingUrl}>Termin buchen</a></section><SiteFooter /><a className="mobile-call-bar" href={site.bookingUrl}><span aria-hidden="true">↗</span>Termin buchen</a></main>;
}
